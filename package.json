{
  "name": "mm32-website",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "MechMania 32 website. Static pages are served as-is; only visualizer/ is built.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "pixi.js": "^8.6.0"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
